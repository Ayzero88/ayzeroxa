import React from 'react';

const AppName = ({fs}) => {
  return (
    <div>
      
      <h1 className='app-title' style={{fontSize: fs}}>FLOCK<span>TRACKER</span> </h1>
    </div>
  )
}

export default AppName;